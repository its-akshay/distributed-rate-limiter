version 1.4:
lua_sliding_window.go acts as a bridge between Go and Redis Lua script,
executing the rate limiting logic atomically and returning allow/reject.


Execute all Redis operations atomically using Lua scripts.
Operations executed atomically:
Remove expired requests
Count requests
Insert new request
Set expiry
This guarantees consistent rate limiting across multiple service instances.

100 requests per 60 seconds


Lua is checking whether a request should be accepted or rejected



v0.1  Core Rate Limiter
v0.2  Sliding Window + Redis
v0.3  Lua Atomic Limiter
v0.4  Dockerized
v0.5  Prometheus Metrics
v0.6  Grafana Dashboard
v1.0  Auto Migrations + Swagger
v1.1  Kubernetes Deployment

v1.2 persistent volume + HPA
    add Kubernetes deployment with ConfigMap, Secret and PVC

v1.3 Liveness Probe: Ensures the application is running and responsive. 
     Readiness Probe: Ensures the application is ready to serve requests. 

v1.4 Add: Ingress
curl -> localhost:8080 -> Port Forward -> service -> pod

becomes=>

curl->rate-limiter.local->Ingress Controller->Ingress Rules->Service->Pod